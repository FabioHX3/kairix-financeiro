# =============================================================================
# Kairix Financeiro - Configuração de Ambiente
# =============================================================================
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o arquivo .env no repositório!

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8014
DEBUG=false
ENVIRONMENT=development  # development, staging, production

# CORS - Origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,https://financeiro.kairix.com.br

# -----------------------------------------------------------------------------
# Database (OBRIGATÓRIO)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://usuario:senha@localhost:5432/kairix_financeiro

# -----------------------------------------------------------------------------
# Security (OBRIGATÓRIO)
# -----------------------------------------------------------------------------
# Chave secreta para JWT - DEVE ter no mínimo 32 caracteres
# Gere uma chave segura: openssl rand -hex 32
SECRET_KEY=sua-chave-secreta-com-pelo-menos-32-caracteres

# Algoritmo JWT (padrão: HS256)
ALGORITHM=HS256

# Tempo de expiração dos tokens (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# API Key para sistemas externos (opcional)
SYSTEM_API_KEY=

# Secret para validação HMAC de webhooks (recomendado em produção)
# Gere: openssl rand -hex 32
WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# LLM - OpenRouter
# -----------------------------------------------------------------------------
OPENROUTER_API_KEY=sua-api-key-do-openrouter
OPENROUTER_MODEL=google/gemini-2.5-flash

# -----------------------------------------------------------------------------
# Redis (para cache e sessões)
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# WhatsApp API (UAZAPI)
# -----------------------------------------------------------------------------
WHATSAPP_API_URL=https://api.uazapi.com
WHATSAPP_API_KEY=sua-api-key-uazapi
WHATSAPP_INSTANCE=nome-da-instancia
